# Copy to .env before first start (install-control-plane.sh does this).
MONGO_ROOT_USER=hermesRoot
MONGO_ROOT_PASSWORD=changeMeNow
HERMES_ENROLL_TOKEN=
# Public/reachable hostnames agents will use in bootstrap mongo_uri:
HERMES_MONGO_HOSTS=localhost:27017,localhost:27018,localhost:27019
